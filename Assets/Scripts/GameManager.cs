using Unity.VisualScripting;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    /*
    [SerializeField] private float timeWaitBeforeExit;
    public void OnPlayerDied()
    {
        Invoke(nameof(EndGame, timeWaitBeforeExit));
    }
    */
}
